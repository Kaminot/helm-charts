apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akvorado.fullname" . }}-test-connection"
  labels:
    {{- include "akvorado.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akvorado.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
